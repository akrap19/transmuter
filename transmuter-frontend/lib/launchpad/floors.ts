/** UI / wizard ASK. Sizes the raise. Does not void on-chain. */
export const TREASURY_ASK = 0.10;
/** On-chain accept floor after USDC→SOL slippage. Do not drive the wizard with this. */
export const TREASURY_ACCEPT = 0.08;
/** On-chain combined treasury + LP cash floor as a share of MCP. */
export const COMBINED_BACKING_MIN = 0.18;
/** Minimum LP allocation of supply (spec r41; was 15%). */
export const LP_ALLOC_MIN = 10;
