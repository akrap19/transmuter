---
description: No secrets in code. Env only. Never log private keys or RPC keys.
alwaysApply: true
---

# Secrets

No secrets in source. Environment variables only.

Never log private keys, payer keypairs, or RPC API keys.
