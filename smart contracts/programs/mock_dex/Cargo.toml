[package]
name = "mock-dex"
version = "0.1.0"
description = "Constant-product swap mock for finalize CU measurement"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "mock_dex"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = "0.32.1"
anchor-spl = "0.32.1"

[package.metadata.solana]
tools-version = "v1.52"
